# NOTE You should consider running the Consul UI on selected Consul hosts rather than all hosts.
# @see https://discuss.hashicorp.com/t/consul-web-ui-tls-enabled/24797/6
# TODO: probably should move this to cli so it can be enabled programmaticly
ui_config {
  enabled = true
}
